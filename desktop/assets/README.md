# Desktop icon provenance

`icon.svg` is a project-owned YHC vector drawn in this directory. `icon.png` is a
512px PNG rendered locally from that SVG with macOS `sips`; no upstream product or
third-party product logo is reused.
